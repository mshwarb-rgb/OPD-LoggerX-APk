{
  "name": "opd-loggerx-apk",
  "version": "1.0.0",
  "private": true,
  "description": "Self-contained offline APK for OPD LoggerX with native CSV/XLS export",
  "scripts": {
    "android": "npx cap open android",
    "copy": "npx cap copy",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/share": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
